## 2026-08-08 21:00
- 依頼: 6件。【A】(最優先)地の文の翻訳が壊れる問題（固有名詞・既に日本語化済みの文字列がプレースホルダ保護されず翻訳APIに渡っている疑い）を原因特定の上で修正。【B】前回提案した「translatePage()を定期再スキャンに組み込む」修正案を採用・実装。二重翻訳防止・順序保証の方法・体感速度を報告。React hydrationエラーは原因として確定させない。【C】効果文の内側タグ（<b>等）も含めて白文字にする。修正後、実機に残る色分岐があれば報告。【D】site-labels.jsonに7件追加（Character/Characters長い方優先、数値サフィックス・数値プレフィックスパターン対応込み）。【E】aosns装備名32件をglossary.jsonにconfirmedで登録、対応不可分は推測せず一覧化。【F】A〜E完了後、全タブを開いた状態でMagnaページ全体の残存英語を洗い出し、報告のみ
- 実施:
  - 作業用ブランチ fix/placeholder-protection-and-full-cleanup を作成
  - 【A】「ヴュンゼーランド」「文字」等の再現テストを実機で実施し、原因を2点に特定: (1) キャラクター名（Magna）はglossary.json（ゲーム用語限定）にもsite-labels.json（UIラベル限定）にも属さないため一切保護されず翻訳APIに渡っていた。(2) 既に日本語になっている文字列（用語置換の先行適用・別処理の痕跡）がそのまま英文中に混入した状態で翻訳APIに渡り、再翻訳されて破壊されていた。CHARACTER_JA_NAMES（Magna限定）とbuildTranslationResolved()で(1)を、任意の仮名漢字連続をプレースホルダ化するsubstituteJapaneseRunPlaceholders()で(2)を解消。護protection適用後・未適用の同一文を実機のTranslator APIに通す比較テストで、保護なしでは実際に「ヴュンゼーランド」「1 文字」といった破壊が再現し、保護ありでは "ヴァンガード クラス"・"5✦ 文字" のように正しく保持されることを確認した。副次的に発見: Prydwenのソースhtmlに `<strong>Use the tabs</strong>to`（スペース欠落）という表記ミスがあり、タグ境界をまたいで単語同士が結合され「tabsto」という未知語になっていたことが「Tabs」が英語のまま残る原因の一つだったため、タグ境界で単語文字同士が接するときのみ空白を補う処理を追加。なお「character」が「文字」と誤訳される件は、この文単体で再現・確認した上でプレースホルダで保護できない一般語の訳語選択の問題（Translator APIの単語義曖昧性）であり、修正不可能なことを確認した（文全体が1つの意味単位として翻訳APIに渡っていること自体は正しく機能している）
  - 【B】提案済みの修正案を実装: translatePage()を、run()と同じ定期MutationObserver再スキャン（500msデバウンス）の中に組み込み、collectTranslationBlocks側の既存のdata-czn-translated除外により新規ブロックのみを対象にする（二重翻訳は起きない）。順序保証は、初回パスと同じ「翻訳(.then)→processPage」という構造を再スキャン側にもそのまま踏襲することで担保しており、非同期チェーン全体が完了してからpendingフラグを下ろすことで、再スキャン同士のオーバーラップも防いでいる。実装中、site-labels側にも同じ「初回のみ実行」問題があることが判明したため（"Introduction"見出し付近に置換済みspanが1つも無いことを実機で確認）、同じ再スキャンにtranslatePageより前の順で組み込んだ。React hydrationエラー#418は、今回も再現・特定はできておらず、原因として確定させていない
  - 【C】applyReadableEffectStyle()に、対象div配下の全descendant要素へstyle.setProperty('color', '#fff', 'important')を適用する処理を追加。Prydwen側が<b>/<u>タグに個別の色指定（computed color rgb(24,122,176)）をしており、親のcolor指定がCSS継承で上書きされないことが原因だったため。実機で現在処理済みの効果div 120件全てを走査し、色分岐が残る要素が0件であることを確認
  - 【D】docs/site-labels.jsonにBuild/Builds/Character/Selected/Level/Debuffer/Counterattackerの7件を追加（52件）。replaceSiteLabelsOnPage()を、大文字小文字を区別しないマッチングに変更（Prydwen側のCSS text-transform: uppercaseにより、実際のDOMテキストは小文字なのに画面表示は大文字になるケースがあるため、実機で生テキストを確認済み）。加えて数値サフィックス（"Stats (level 60)"→"ステータス（レベル60）"、スペース無し連結）と前後の半角括弧の全角化を検出・変換する処理を追加。実機で"Stats (level 60)"→"ステータス（レベル60）"、"[6 selected]"→"[6 選択中]"を確認
  - 【E】Prydwenの装備欄（アイコン+名前のみ、効果文表示なし）とaosnsの装備一覧（効果文付き）を比較し、これまでのカード名照合で使ってきた「効果文の内容一致」による突合方法が装備名には使えないことを確認。バイリンガルの一覧も存在しないため、32件（武器4/防具14/装飾14）いずれも名前の類似度だけによる推測になってしまうため登録を見送った。glossary.jsonは今回無変更（confirmed 368のまま）
  - 【F】全5タブ（KIT/REVIEW/EPIPHANIES/GEAR/DECK & TEAMS）をクリックしてDOMに反映させた状態でページ全体を走査し、最新のglossary.json（confirmed 368件）・docs/site-labels.json（52件）と突き合わせ。短いブロック（6語未満のラベル・見出し等）165件中99件が未登録として一覧化した（サイトラベル10種、部分一致のゲーム用語7種、対象外の固有名詞82件）。新たに、"Divine Epiphanies:"（複数形）が登録済みの"Divine Epiphany"（単数）と完全一致せず「神 Epiphanies:」と分離表示される不具合を発見（未修正・報告のみ）。実機の拡張機能は自動化ツールでは再読み込みできないため、本日の修正を反映する前のバージョンの処理痕跡と新しい辞書が混在した状態での走査である旨を明記した
  - 【B】の体感速度測定: 拡張機能を自動化ツールで再読み込みできないため、実際の「タブを開いてから」のエンドツーエンド計測はできなかった。代わりに、ページに存在する本物のTranslator API（window.Translator、拡張機能のバージョンに依存しない）に対し、Magnaの実データに近い10文（6〜20語程度）を逐次翻訳させて実測: 平均25ms/文（範囲19〜30ms）。create()のインスタンス生成は既にモデルがロード済みの状態で0ms。この実測値と、既知のページ全体の長文ブロック数（138件、前回洗い出し時点）から推計すると、全ブロックを逐次処理しても合計で3〜4秒程度であり、KITタブの内容がその中の何番目に位置するかによって、再スキャンのMutationObserverデバウンス（500ms）+ その位置までの逐次処理時間（25ms×位置）だけ遅れて日本語が表示される計算になる。実測ではなく実データと実APIを組み合わせた推計である旨を明記して報告する
- 結果:
  - node --check・制御文字スキャン・sync.pyすべて通過。effects-ja.json・docs/bookmarklet.js・glossary.jsonは無変更
  - docs/site-labels.json: 45件→52件
- 未解決: 【B】拡張機能を自動化ツールで再読み込みできないため、体感速度は実測ではなく推計。【E】aosns装備名32件は未登録のまま（信頼できる突合方法が無いため）。【F】"Divine Epiphanies"（複数形）の不一致、および洗い出した99件の未登録語は未対応
- コミット: 4f46775
