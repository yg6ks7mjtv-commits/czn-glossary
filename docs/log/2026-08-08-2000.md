## 2026-08-08 20:00
- 依頼: ユーザーがDevToolsで取得した3つの事実（CZN_DEBUGログが出ない、Translator API自体は呼ばれている、React hydrationエラー#418）を踏まえ、【A】診断を常時見える形にする（最優先）、【B】Reactによる上書き仮説を検証（原因特定後は実装前に報告）、【C】効果文の色分岐の原因を実機で特定（報告のみ）
- 実施:
  - 作業用ブランチ fix/react-hydration-and-diagnostics を作成。ブラウザ拡張が接続可能な状態に復帰
  - 【A】CZN_DEBUGに依存しない常時出力のlogAlways()を追加し、AI翻訳フェーズのstatus・件数の診断行をconsoleに必ず出すようにした（トーストは補助として維持）。既存のlog()自体やCZN_DEBUGの既定値は変更していない
  - 【B】実機のMagnaページで、翻訳対象ブロック収集ロジックを今この瞬間のDOMに対して実行したところ、POTENTIAL欄の説明文4件が問題なく収集対象になることを確認（収集ロジック自体に不具合は無い）。一方、ページ上の実際の`[data-czn-translated]`要素140件のうち、POTENTIAL・カード・凸などを含む「KIT」タブパネル（.tab-inside.active）配下は0件だった。同じタブパネル内には`[data-czn-done]`（カード名・効果文・用語置換）が56件存在し、こちらは正常に処理されていた。translatePage()はページ読み込み時に一度だけ実行され、run()のような定期的な再スキャン（MutationObserverによる500ms間引き再実行）には組み込まれていない。これらを総合すると、KITタブパネルの内容はrun_at:"document_idle"の時点でまだDOM上に存在せず（Reactのハイドレーション遅延・タブ関連の描画タイミングによるものと推測される）、一度きりのtranslatePage()の実行時には収集対象に入らず、その後タブパネルが実際にマウントされた時点では再翻訳の機会が無いまま取り残されている可能性が高いと判断した。ただしReact hydrationエラー自体は今回のセッションでは再現できておらず、「Reactが書き換え済みDOMを破棄して上書きしている」という仮説そのものは確認も否定もできていない（コンソールにエラーが出ない2回のページ読み込みで検証したが、状況依存で毎回起きるとは限らない）
  - 併せてsite-labelsの動作確認も行ったが、実機で開いたページでは「INTRODUCTION」が英語のまま残っており、site-labels機能が有効化される前のバージョンの拡張機能が読み込まれている可能性がある（コミット時期の特定はできていない）
  - 【C】氷の破片III（複数行構造のカード）で実機のDOM・computed styleを確認した。可読性スタイル（白文字!important）は外側の.skill-with-coloring divには正しく適用されているが（computed color: rgb(255,255,255)）、日本語効果文が実際に入っている内側の<b>タグはcomputed colorがrgb(24,122,176)（Prydwen独自の青系ハイライト色）になっていることを確認した。CSSの継承は「子要素に直接かかるルールが無い場合のみ」有効になるため、Prydwen側が<b>タグに個別の色指定をしている場合、親のcolor（!important含む）は子には継承されず、子自身の色が優先される。これが「上段・下段で色が違う」現象の少なくとも一因であることを確認した（このカードでは可視テキストが1行に収まっており、ユーザー報告の「上段/下段2行」とは一致しない可能性があるため、完全に同一の現象かは未確認）
- 結果:
  - 【A】実装済み。次回のトーストとconsole両方でstatus・件数が確認できる
  - 【B】原因の断定はできなかったが、「KITタブパネルの内容がtranslatePage()実行時点でDOMに存在しなかった」ことを示す強い状況証拠を得た。修正案（後述）は未実装
  - 【C】<b>タグの独自色指定が親のcolor指定より優先されることを実機で確認・報告。修正は行っていない
- 未解決: 【B】の根本原因（Reactのハイドレーション遅延か、hydrationエラーによる部分的な再描画か）は未確定。修正案として、translatePage()の対象収集を（run()のカード処理と同様に）既存の定期的な再スキャンにも組み込み、data-czn-translatedが無い新規ブロックだけを都度拾い直す方式を提案するが、ユーザー確認前のため未実装。【C】の「上段/下段」現象がこのカードと同一のものかは未確認
- コミット: b997922（Aのみ実装。B/Cはコード変更なし・報告のみ）
