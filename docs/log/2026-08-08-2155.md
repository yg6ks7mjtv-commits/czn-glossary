## 2026-08-08 21:55
- 依頼: 7件。【A】(最優先)デッキ解説欄の本文が英語のまま残る原因を特定し修正。【B】保護した日本語プレースホルダが文末に移動する現象を調査し、直せるなら直す・技術的に困難なら限界を報告。【C】glossary.json/site-labels.jsonの英語用語も翻訳前にプレースホルダ保護する。【D】site-labels.jsonに18件追加（Ego 1〜6の数値対応、T1/T2/T3等は追加しない）。【E】部分一致だった用語3件の修正とDMG%/HP%の調査、Lush Seaweed 防具の再確認。【F】装備名の突合を別の手立て（Prydwen専用装備ページ）で再挑戦。【G】A〜F後、長文ブロックも含め全タブ展開状態で残存英語を洗い出し・報告のみ
- 実施:
  - 作業用ブランチ fix/translation-full-coverage-and-equipment-match を作成
  - 【A】実機で2つの原因を特定: (1) 逐次翻訳処理がページの出現順そのままでブロックを処理するため、ページ下部（デッキ解説等）は先に何十件も処理し終えるまで手が付かず、実機で最大15秒前後、日本語化されずに見える時間があることを確認。(2) ページ内に同一の「デッキ解説」見出しを含む完全に同じ構造のDOMが2箇所存在し（片方は`div#S:1`というNext.jsのSuspense境界的なid配下）、60秒経過後も消えない恒常的な構造であることを確認。これにより翻訳対象ブロック総数がほぼ倍（140→278件）になっていることも判明。この重複DOMの発見は、以前から未確定のままのReact hydrationエラー(#418)仮説に関連する新しい具体的傍証だが、確定はしていない。対症療法ではなく、prioritizeVisibleTabBlocks()を追加し、アクティブな（今見ている）タブパネル内のブロックを非アクティブなタブパネル内のブロック（重複DOM側も含む）より先に翻訳する変更を実装。タブ切り替え自体はclassの付け替えのみでMutationObserverが発火しないため、タブクリックでも同じ再スキャンを起動するリスナーを追加。実機でソート順が正しくアクティブタブ優先になることを確認
  - 【B】実機のTranslator APIに同じブロック構造（複数文・同じ保護語が2箇所出現）を繰り返し入力し再現テストを実施。同じ入力でも大半は正しい位置に保持され、まれに文末への大きなズレが発生することを確認（既知のTranslator API非決定性と一致）。プレースホルダで保護した語の文法的役割をモデルが認識できないことに起因する、現行のプレースホルダ方式そのものの限界と判断し、指示通り今回は実装を見送り、原因と限界を報告のみとした
  - 【C】原因を特定: glossary.jsonの用語は既存のsubstituteTermPlaceholdersで保護されていたが、site-labels.jsonの用語（buildTranslationResolvedがglossary.json由来のみをマージ）は保護対象外だった。「Character」がsite-labels.jsonにしか登録されていないため、"5-star character"が無保護のまま翻訳され「文字」に誤訳されていた。substituteSiteLabelPlaceholders()（大文字小文字を区別しない）を追加しtranslateOneBlock/translateBlocksSequentially/translatePageに引き回した。実機で「character」が正しく「キャラクター」になることを確認
  - 【D】Kit/Review/Gear/Deck & Teams/Chaos Mode/Chaos Teams/Generic - Chaos/Counter-Attack/Fragments/ST Damage/AoE Damage/AP Efficiency/Utility/Removal Priority/Conditional Card Removal/Copying Priority/Base/Egoの18件を追加（52→70件）。「Ego N」の数値サフィックスは既存の汎用ロジック（特定キーに限定されない設計）でそのまま対応。T1/T2/T3・x2・DPS-Lite・HP・DPSは指示通り追加せず
  - 【E】Divine Epiphanies（複数形）=神ヒラメキ、Health Proficiency=HP熟練、Unique Stat Upgrade=固有能力値強化をconfirmedで追加。aosnsのMain/sub-stats表を直接確認し、HP%はaosns側でも「HP%」と英語表記のまま（登録見送り）、DMG%は単独表記の用例が無く「正義属性ダメージ量」のような属性名込みの複合語でしか登場しないため推測登録せず。「Comments - Weapons/Armor/Trinkets」を実機で再確認したところ、旧バージョンの痕跡ではなく構造的な原因と判明: Prydwenの生HTMLが装備種別の単語（Armor等）だけを独立した`<span>`で囲んでおり、テキストノード単位でしか一致しないreplaceSiteLabelsOnPageでは、フレーズ全体の登録（プロジェクト最初期から登録済みの3件）が要素境界をまたぐため一度も一致していなかったことが判明（「Comments - 防具」と部分的に見えるのは、単独語として別途confirmed済みのArmor=防具がglossary側で当たっているだけ）。今回の指示範囲外のため修正はせず報告のみ
  - 【F】Prydwenに専用の装備データベースページ（/chaos-zero-nightmare/equipment、アイテムごとの効果文・レア度別ステータス推移あり）が存在することを発見。aosnsにも同様の専用「装備一覧」ページ（効果列あり）があり、カード名で実績のある効果文突合法を適用した。数値のステータス推移はレア度・部位ごとの共通テーブルで一意ではなかった（複数アイテムが同じ数値列を共有）ため、主に効果文の内容一致で判定し、名称の直訳一致を補助的な確認とした。32件中21件（武器4・防具10・装飾6、および以前から他エントリのnote内でのみ言及されていたMultifaceted Parallel Universe Nexusを含む）を確認し登録。5件（Verdant Shackles、Quantum Collector、Sphere of Randomness、Harp of Conflict、Starlight and Dreams）はaosns装備一覧に対応する効果文が見つからず、推測せず未登録のまま報告
  - 【G】全5タブを展開した状態で、今回のセッションの最新コード（辞書・保護・優先順位付けすべて含む）を実機DOMに直接注入して独立検証した（インストール済みの拡張機能に頼らない）。長文ブロック102件を実際にTranslator APIで翻訳し102件成功。新たな要対応候補としてAoE/AOE（未登録・大文字小文字表記ゆれ）、Neutral+Epiphany（未登録複合語）、"Last ... update"定型句（単語単位のsite-label適用のみで定型句自体は未登録）、"Card N"数値サフィックス見出し、Ads（サイト広告関連の可能性、優先度低）を発見・報告。また「Card Removal Priority」等の見出しで、既存のCard=Card(カード)と今回追加したRemoval Priority=抜くカードの優先度が隣接適用され「カード」が二重に見える表示上の冗長性を発見・報告（誤訳ではないため未修正）。走査結果の一部（Health Proficiency・Divine Epiphanies）は、インストール済み旧バージョンによる先行置換でテキストノードが分断された影響であり、単独テストでは正しく動作することを別途確認済みである旨を明記した
- 結果:
  - node --check・制御文字スキャン・sync.pyすべて通過。effects-ja.jsonは無変更
  - glossary.jsonのcoverage: confirmed 368→392 / total 475→499
  - docs/site-labels.json: 52件→70件
- 未解決: 【B】プレースホルダ語順ズレは現行方式の限界として未対応（次善策は無し）。【E】Comments - Weapons/Armor/Trinketsのフレーズ単位site-labelは構造上一度も一致しない不具合として現存（修正は指示範囲外のため見送り）。【F】装備名5件は未対応のまま。【G】AoE/Neutral+Epiphany/"Last...update"定型句/Card N見出し/Ads、および「Card」二重表示の冗長性は未対応（報告のみの指示のため）
- コミット: cd99ec6
